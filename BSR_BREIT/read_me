bsr_breit       -   a little extended CPC version with ZCONF90 library (alias b10)

bsr_breit3      -   new version with ZCONFLS and SHELLS   (alias b30)

bsr_breit3_mpi  -   mpi version for bsr_breit3

bsr_breit4_mpi  -   reduce the limits on NTERMS and write results as 
                    coef(i,j),ihm(i),jhm(i),intc(j),idfc(j)
                    So, different int_bnk files !!!

I change 4 to include both record option based on paramters irec - should be checked
